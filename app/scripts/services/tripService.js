'use strict';

angular.module('plannerServices')
  .service('Trip', function Trip() {
    
    /**
     * Save trip
     * - At first term, in local storage
     * @return {boolean} True if all was good
     */
    this.save = function ( trip ) {
      trip = trip;
      return false;
    };


  });
